# Copyright(C) 2026 Advanced Micro Devices, Inc. All rights reserved.
# SPDX-License-Identifier: MIT

# ROCm 7.14 base (rocm/pytorch torch 2.10.0), the current standard for these ports.
init_image: rocm/pytorch:rocm7.14_ubuntu24.04_py3.12_pytorch_release_2.10.0

build_arguments:
- "COSMOS_COMMIT=0fa3ba479a7c1c5be28f81fe084b9ea544d697c9"

gpu_support: true
x11_display: false

environment_variables:
- "HSA_OVERRIDE_GFX_VERSION=11.5.1"
- "HF_HOME=/root/.cache/huggingface"
- 'HF_HUB_DISABLE_XET=${HF_HUB_DISABLE_XET:-1}'
- 'HF_HUB_ENABLE_HF_TRANSFER=${HF_HUB_ENABLE_HF_TRANSFER:-0}'
- 'HF_HUB_DOWNLOAD_TIMEOUT=${HF_HUB_DOWNLOAD_TIMEOUT:-60}'
- "HF_HUB_DISABLE_TELEMETRY=1"
- "TOKENIZERS_PARALLELISM=false"
- "LD_LIBRARY_PATH="
- "TORCH_ROCM_AOTRITON_ENABLE_EXPERIMENTAL=1"
- "TORCH_BLAS_PREFER_HIPBLASLT=0"
- "OUT_DIR=/outputs"
# Gated NVIDIA HF repos (weights + dataset) need a token.
- 'HF_TOKEN=${HF_TOKEN:-}'
# Per-demo / eval knobs (override from the host: VAR=... ryzers run /ryzers/demos/demo_*.sh).
- 'SEED=${SEED:-}'
- 'TAG=${TAG:-}'
- 'NUM_EPISODES=${NUM_EPISODES:-}'
- 'NUM_STEPS=${NUM_STEPS:-}'
- 'CKPT=${CKPT:-}'
- 'PORT=${PORT:-}'

volume_mappings:
- "$PWD/workspace/cosmos3/outputs:/outputs"
- "$PWD/workspace/cosmos3/models:/models"
- "$PWD/workspace/.cache/huggingface:/root/.cache/huggingface"
